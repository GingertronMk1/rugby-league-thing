name: tests

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main

jobs:
  ci:
    runs-on: ubuntu-latest
    environment: Testing

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install
        run: ./bin/install
      - name: Test
        run: ./bin/testing
      - name: Pint
        run: ./vendor/bin/sail run ./vendor/bin/pint
